DriftSlide — Change Log
=======================

1.0.0 (2026-06-23)
-------------------
Initial release.

* OBS source plugin ("DriftSlide") registering as OBS_SOURCE_TYPE_INPUT.
* 4-state display cycle: Transparent → Fade In → Displaying → Fade Out.
* Configurable durations: transparent gap (1–3600 s), display (1–300 s),
  transition (0.1–10 s).
* Eleven transition styles across three families:
  - Fade: pure alpha dissolve.
  - Slide Left / Right / Up / Down: image enters from one edge and exits
    back the same side.
  - Scroll Left / Right / Up / Down: continuous flow — image enters from
    one edge and exits the opposite side.
  - Zoom: image starts at 1.67× crop and settles to full size.
  - Zoom In: image grows from a single point at the centre outward.
* Ken Burns mode: gentle slow pan and zoom throughout each image's entire
  visible lifetime, with randomised direction per image.
* Directory scan with case-insensitive extension matching for PNG, JPEG, BMP,
  TGA, WebP, and GIF files.
* Alphabetic and random (Fisher-Yates shuffle) image ordering.
* Thread-safe settings handoff from UI thread to render thread via a
  pending-values / dirty-flag pattern.
* Premultiplied SRGB image loading and correct blend mode
  (GS_BLEND_ONE / GS_BLEND_INVSRCALPHA).
* Explicit ViewProj uniform declaration in the OBS effect file, required by
  the D3D11/HLSL compiler on Windows (OpenGL injects it automatically).
* OBS_SOURCE_CUSTOM_DRAW added to output_flags so the plugin's custom effect
  does not conflict with OBS's own default effect on the render thread.
* Texture bound with gs_effect_set_texture (not _srgb) to avoid a second
  sRGB→linear decode on already-linear premultiplied texture data, which
  caused images to appear too dark on Windows.
* Smooth easing via smoothstep on all transitions.
* Zero pixel output during the transparent interval — no black frames composited
  into the scene.
* UTF-8 path encoding when passing file paths to OBS image loader (fixes image
  discovery on Windows for directories with non-ASCII characters).
* Scan result logged to the OBS log at INFO level to aid debugging.
